# -*- coding: utf-8 -*-
"""
تنظیمات اتصال به ایتایار (eitaayar.ir).

⚠️ توجه مهم (حتما بخوانید): طبق مستندات رسمی eitaayar.ir (که در پروژه هم
ضمیمه شده)، این API فقط سه متد دارد: getMe، sendMessage، sendFile — هر سه
فقط برای «ارسال» پیام از طرف ربات هستند. هیچ متد مستندی برای «دریافت»
پیام‌های کاربران (نه webhook، نه getUpdates) در این API وجود ندارد. برای
همین، بر خلاف تلگرام و بله، اینجا نمی‌شود منوی شیشه‌ای تعاملی یا دیده‌بان
شخصی ساخت — چون ربات اصلاً نمی‌فهمد کاربر چه پیامی فرستاده یا کدام دکمه را
زده. آنچه اینجا پیاده شده «ارسال گزارش نرخ به کانال ایتای شما» است، نه یک
ربات مکالمه‌ای کامل. اگر بعداً مستندات رسمی‌تری از یک متد دریافت پیام
(polling یا webhook) پیدا کردید، به من بدهید تا این بخش تکمیل شود.
"""
import os

EITAA_TOKEN = os.environ.get("EITAA_TOKEN", "").strip()

# شناسه یا یوزرنیم (بدون @) کانال/چتی که گزارش نرخ‌ها در آن پست می‌شود.
# می‌تواند همان کانال نرخ امروز چند شما باشد.
EITAA_CHAT_ID = os.environ.get("EITAA_CHAT_ID", "nerkhemroozchand").strip()

EITAA_API = f"https://eitaayar.ir/api/{EITAA_TOKEN}"
